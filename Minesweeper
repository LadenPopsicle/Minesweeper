import java.util.Scanner;
import java.util.Scanner;

public class Minesweeper{
  System.out.println("Enter the number of rows.");
  Scanner kb = new Scanner(System.in);
  int rowCols = kb.nextInt();
 
  int numMines = (int) ((Math.random() * ((25 - 15) + 1)) + 15; ))   //number of mines randomly chosen to be between 15 akaong 
 
  public static String[][] genPuzzle(side, mine){ 
    Random r = new Random();
    String[][] arr = [side][side];  
    int counter= 0; //I guess we can use this to keep track of how many mines there are already while we add them
    while(counter<mine){
      int checkX = r.nextInt(side);
      int checkY = r.nextInt(side);
      if(!((arr[checkX][checkY]).equals("#"))){
        arr[checkX][checkY] = "#";
        counter ++;
    }
   }
   // The above while loop placed the mines 
 }

 
  String[][] grid= genPuzzle(rows, cols, numMines);

  for(int i = 0; i < grid.length; i++) {
  for(int j = 0; j < grid[i].length; i++) {
   if(grid[i] ==
      
      }
}
